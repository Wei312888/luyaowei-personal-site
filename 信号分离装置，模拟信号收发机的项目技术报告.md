# 电赛报告全文整理

> 本文档整理自两份电赛报告：
> 1. **全国大学生电子设计竞赛 H 题 — 信号分离装置**（PDF；赛题年份 2023，对应站点“宽带混合信号分离与锁相重建系统”项目，完成于 2026.05）「修订注：原文为“2023年全国大学生电子设计竞赛 H题 — 信号分离装置（日期 2023-08-05）”；赛题年份 2023 保留，项目完成时间按简历口径修正为 2026.05」
> 2. **2026年全国大学生电子设计竞赛 B题 — 模拟信号收发机**（DOCX）

---

# 第一部分：信号分离装置（H题）

**2023 年全国大学生电子设计竞赛赛题（H 题）**
**题目：信号分离装置（H题）**
**完成日期：2026 年 5 月（2026.05）**「修订注：原文为“2023年全国大学生电子设计竞赛／日期：2023年8月5日”，其中 2023 为赛题年份；本项目按简历口径完成于 2026 年 5 月，对应站点“宽带混合信号分离与锁相重建系统”项目」

---

## 摘要

本系统由加法器、分离电路组成。输入信号 A 和 B 经增益为 1 的加法器得到信号 C。后级分离电路由单片机采样 C 信号并使用 FFT 分析得到 A、B 信号参数，控制两路 DDS 模块分别输出与信号 A，B 同频同形的偏置信号，后经放大电路和交流耦合电路输出 A'和 B'。单片机通过相位反馈方式，使用 PID 算法精细控制 DDS 信号频率，实现波形同频显示。

此系统可以在 1s 内对 20kHz-100kHz 的不同频正弦信号或三角波信号分离，并在题给条件下输出相位偏移，显示输入信号类型和频率，已实现题目的基本要求和发挥部分的所有功能，在发挥部分（2）之上实现了三角波的相位控制、扩大工作范围至 5kHz-100kHz 等。

**关键词：** 加法器；FFT；PID；DDS

---

## 一、系统方案

本系统由加法器和分离电路组成。下面分别对我们提出的方案进行论证与选择。

### 1、加法器电路方案论证与选择

#### （1）加法器混合信号方式论证与选择

**方案一：使用同相加法电路。** 同相加法器在原理上能够实现信号的相加。但是实际运用中两路输入信号之间会相互干扰。这是因为同相加法器的输入阻抗比较高，信号不易流入加法器的输入端，会引起输入信号间的相互干扰。

**方案二：使用反相加法+反向跟随电路。** 反向加法电路的输入阻抗小，性能稳定，信噪比较大，缺点是输出信号反相，如果要实现正向相加的输出，还需要再接一级反相器。

综上所述，出于性能考虑，我们选择了**反相加法+反向跟随电路**的形式实现。

### 2、分离电路方案论证与选择

#### （1）信号识别方式论证与选择

**方案一：使用模拟电路器件**，低通滤波器对信号 C 进行滤波，获得频率较低的信号 A'，再使用减法器把信号 C 与 A'进行相减操作，获得信号 B'。本方案流程简单，无需过多的程序设计。但弊端在于，滤波器的相应特性适用于分离单频信号，即正弦信号；对于三角波等带有谐波的信号，若混有与基波或谐波相近频率的其他信号，较难分离。

**方案二：使用 STM32F407VET6 板载 ADC** 对信号 C 的波形进行采样，并使用 FFT 进行时域到频域的转换。通过分析频域上的特征，将信号 A 和 B 区分开来。本方案的主要工作量在于软件部分。根据不同波形的基波和谐波特征，可以有效实现波形的判断和分离。

综上所述，我们选择**方案二**作为信号识别方式。

#### （2）信号变换方式论证与选择

在（1）方案二的基础上，我们选择定时器触发 ADC 采样。因此，如何选择主控 MCU 时钟频率和 FFT 的采样频率仍要权衡，我们有以下两种方案。

**方案一：** 把主控 MCU 的主频设置到最高值 168MHz，再设置定时器分频 164 倍，使得采样频率逼近 1.024MHz。但弊端在于，这样采集出来的信号 FFT 之后得到的频域波形会发生频谱泄露，需要加窗函数才能修正频谱。并且，修正之后的波形也不够精确，会影响后面对信号的处理。

**方案二：** 牺牲主控 MCU 主频，调至 128MHz，再设置定时器计数值为 125，这样得到的采样频率恰好等于 1.024MHz，FFT 变换后的频谱不会发生泄露，方便后面对信号进行处理。

综上所述，为了避免频谱泄露，便于处理数据，我们选择了**方案二**。

#### （3）信号输出方式论证与选择

**方案一：使用模拟方式**将波形进行滤波、相减等运算变换后输出。该种方法基于原始信号进行电路处理，输入和输出具有很高的同步性。但如前文所述，模拟方法对于正弦信号之外的其他信号适应度较差，同时滤波器还会带来信号的相移，使用减法器将 C 和 A'相减时，这两个信号的相位已经出现偏差，不能保证 B'信号的还原。

**方案二：使用 STM32F407 单片机的板载 DAC 配合 DMA** 进行输出。这种方法的特点在于成本较低，使用 STM32 即可进行信号输出。但是 STM32F407 的 DAC 性能较差，经过测试，在信号频率 70kHz 以上时输出幅度较小的三角波已经开始失真(每个周期 64 个采样点)，不符合该题目的输出要求。

**方案三：使用 DDS 模块。** 我们结合题目的要求，采用 AD9833 模块进行测试，在题目要求的 20kHz-100kHz 范围内均能够输出无失真的正弦波以及三角波。同时主控对 DDS 的操作简单，只要在初始化以及改变频率、相位时，发送数据字即可进行相应的连续输出操作，占用的系统资源较少，同时 DDS 输出的信号较为精确。

综上所述，我们选择**方案三**作为信号输出方式。

#### （4）频率反馈调节方式论证与选择

**方案一：使用硬件锁相器电路**，对分离得到的低频信号 A 转为方波，并通过锁相器电路将该 20kHz-100kHz 的信号倍频至几十 MHz，作为 DDS 的时钟输入，使 DDS 时钟与输入信号同步，尽可能减小细微的频率差别，达到抑制示波器漂移的目的。但该种方式需要通过硬件将 A'信号分离出来，同时需要自己搭建锁相环电路，工作量较大。

**方案二：先对信号 C 和输出 DDS 信号完成自动频偏纠正。** 再使用软件对频率进行反馈控制。对 DDS 输出信号以及信号 C 分别 FFT 求出相位差并进行 PID 反馈控制，达到控制相位以及抑制漂移的目的。

综上所述，我们选择**方案二**作为频率反馈调节方式。

### 3、系统总体结构

```
信号A ──┐
        ├──> 加法器(C=A+B) ──> 电压抬升 ──> STM32 ADC采样 ──> FFT分析
信号B ──┘                                          │
                                                   ├──> 识别A/B频率和波形
                                                   ├──> 控制DDS_A输出A'
                                                   └──> 控制DDS_B输出B'
                                                          │
                                            DDS_A ──> 放大+交流耦合 ──> A'输出
                                            DDS_B ──> 放大+交流耦合 ──> B'输出
                                                          │
                                            A'、B'电压抬升后反馈到ADC（PID锁相）
```

*图1 系统总体结构图*

---

## 二、系统理论分析与计算

### 1、信号分离原理分析

我们使用了 FFT 提取信号 C 的特征。FFT 是一种 DFT 的高效算法，称为快速傅里叶变换（fast Fourier transform）。通过对信号 C 进行频谱变换，可以提取出信号频谱的幅度、相位等特征。正弦波和三角波各自有相应的频谱特征：

- **正弦波的频谱**：只会在某个频率对应的正负点有幅值和相位。
- **三角波的频谱**：在正负频率点有基波、三次谐波、五次谐波乃至更高次谐波。谐波次数越高，其分量越小。同时基波和谐波的相位关系为同相或反相。

当三角波和三角波、正弦波与正弦波、正弦波与三角波相加时，得到的信号频谱特征会出现不同。分别识别出两个波形的基波及较低次谐波，即可将信号分离开。

题目要求需要分辨信号的频率为 5kHz 的整数倍，且采样的信号基频最高为 100kHz。若三角波基频为 100kHz，要采得五次谐波分量，则要采集的最高频率为 500kHz。根据奈奎斯特定理，采样频率至少为 1MHz。同时为了满足采集信号频率是分辨率的整数倍，故将采样频率定为 **1.024MHz**，进行 1024 点 FFT 时分辨率刚好为 1kHz，变换后的频谱几乎不会发生频谱泄露。

频谱中包含了信号基频和谐波分量信息，可以从这些信息中分离出信号。分离流程如下：

```
采集信号C(1.024MHz, 1024点)
        │
        ▼
    FFT变换
        │
        ▼
  排除直流分量，取前半部分频谱
        │
        ▼
  找两个最大值 → 提取A、B基频
        │
        ▼
  判断谐波分量 → 识别波形类型(正弦/三角)
        │
        ▼
  控制DDS输出对应频率和波形
```

*图4 信号分离流程图*

### 2、同步信号输出

由于我们的系统输入信号和输出信号是由不同的时钟源产生，即使将输入和输出各调节至同一频率值，其产生的信号频率也会有微小的差别，这种差别在示波器上的显示效果是：用于触发的信号稳定不移动时，另外一个信号会出现一定速度的移动。对于这种现象，应该采取措施使输出信号的频率尽可能接近输入频率。

### 3、调节初相位差

我们程序只能得到两个信号各自的相位，而发挥部分（2）要求的是控制初相位差，因此需要将两信号相位经过公式推导得到初相位差。

如果 ADC 从 t₁ 时刻采样，则根据图中关系式可联立出以下公式：

```
t₁ = (φA' / 360°) × TA'
t₁ - τ = (φB' / 360°) × TB'
φ = (τ / TB') × 360°
TA' = n × TB'
```

联立可得：**φ = nφA' - φB'**

如果 ADC 从 t₂ 时刻采样，此时 φB' 不变，而 φA' 发生变化，因此可以换算得到 t₁ 时刻的相位，再代入上式。t₁ 时刻相位与 t₂ 时刻关系式如下：

```
φA1' = φA2' - 360°/n
φA1' = φA2'
```

---

## 三、电路与程序设计

### 1、电路设计

#### （1）加法电路

两路信号从运放反相输入端接入，构成一个增益为 1 的反向加法电路，后接一级反向跟随器，实现信号 C=A+B。电路采用充电宝提供 5V 电压，采用 K-CUT 单电源转双电源模块，产生 ±5V 电压，给运放供电。

*图6 加法电路原理图*

#### （2）电压抬升电路

我们采用数字的方式识别信号，而单片机 ADC 通道只能采集 0-3.3V 的电压信号，因此需要对加法电路得到的信号 C 加上一个约为 1.6V 的直流偏置。

电压抬升电路采用反向加法，直流偏置由电阻分压得到 1.6V 的直流信号，反向加法得到的电压为 -(IN1+VREF)，因此需要在后级接上一个增益为 -1 的反向跟随器，实现电压抬升。

*图7 电压抬升电路原理图*

#### （3）分离电路设计

分离电路由跟随器、放大器、交流耦合、单片机部分、DDS 模块、电压抬升电路组成。

增益为 7.8 倍的放大电路，将 DDS 模块输出的峰峰值为 260mV，带有 140mV 偏置的信号放大到 2V，有源高通滤波器进行交流耦合，最终输出信号 A'和 B'峰峰值 ≥2 V（实测 2.04~2.10 V），无偏置。「修订注：原文为“最终输出信号 A'和 B'峰峰值大于 1V”；按简历口径统一为输出峰峰值 ≥2V」信号 A'和 B'进行电压抬升后反馈到单片机。

*图8 放大器和交流耦合电路原理图*

### 2、程序的设计

#### （1）程序功能描述

程序所有功能都集成在一块主控板上实现。程序可以完美辨认出频率在 5K 到 100K 之间，信号波形分别为三角波或正弦波，并输出同样频率和波形的信号，并可以将结果显示在串口屏上。并且，程序实现输出信号和输入信号的同频显示，且能控制每个通道输出信号与输入信号的相位差。可以通过串口屏设置输出的 A'和 B'信号的初相位差，初相位误差控制 1°之间。

#### （2）程序设计思路

程序整体通过状态机思想实现，每个阶段视作一种状态，并单独设置状态变化条件。

```
┌─────────────┐
│  采集信号    │ 1.024MHz采样1024点
└──────┬──────┘
       │ 采样完成
       ▼
┌─────────────┐
│ 提取原信号   │ FFT找基频+判断波形
└──────┬──────┘
       │ 识别完成
       ▼
┌─────────────┐
│ 自动频偏校正 │ 3路ADC同步采样+FFT求相位差
└──────┬──────┘
       │ 频偏估算完成
       ▼
┌─────────────┐
│ PID闭环锁相  │ 同频模式 / 调节初相位差模式
└─────────────┘
```

*图9 程序状态转换示意图*

**采集信号：** 使用 1024kHz 的采样率采集 1024 个点，使得 FFT 分辨率恰好为 1kHz，使得频谱几乎不会泄露。

**提取原信号：** 是从频谱数组排除直流量的前半部分中找两个最大值，提取出两个信号的基频。得到基频后，通过判断谐波分量来判断两个信号的波形。

**自动频偏校正：** DDS 输出与信号 A、B 基频相同的正弦波，设置同一时钟源触发的 3 路 ADC 以 256kHz 的采样率，用 DMA 采集 256 个数据。对三组数据分别进行 256 点 FFT，实现同步采样。可从 3 个频谱中提取出 A、A'、B、B'四个信号的相位，每一对信号作差，可以得到两个通道各自的相位差。重复该流程 31 次，得到 31 次总相位偏移量，除以 2π 转换为周期，再除以 31 次采样消耗的时间，可以得到频偏。

**PID 闭环锁相环：** PID 闭环锁相环有同频模式和调节初相位差模式。首先，DDS 先输出频率原信号基频加上频偏的信号。然后进入 PID 控制循环，先同自动频偏校正模式一样，得到各个通道的相位差。将相位差作为 PID 的反馈，期望设置为 0，输出差分频率，DDS 输出频率为基频加频偏加差分频率的信号，然后重复循环这个流程，实现闭环锁相。

调节初相位模式是在同频模式基础上，先用 A、B 信号相位，由前文对移相实现的分析，经过换算得到初相位差，由于 A'和 A，B'和 B 的 PID 闭环期望为 0，所以 A'、B'初相位差该等于初相位差。通过设置 B'信号的 PID 期望相位来控制 A'、B'初相位差。

---

## 四、测试方案与测试结果

### 1、测试方案

#### （1）软硬件测试

将系统组装好，连接电源，确认无误后上电。将信号源的两路信号输入到 A、B 输入端，设置峰峰值为 1V。将 A、A'接到一个示波器，将 B、B'接到另外一个示波器。按键触发，观察屏幕上是否能显示对应的波形参数，示波器波形能否同步。

#### （2）性能测试

在（1）的基础上，按下按键，计算从按下按键到输出稳定的时间。在设置相位调整功能的情况下，测量输出的信号 A'、B'间相位的误差值。

### 2、测试仪器

普源 DS1074Z 数字示波器，普源 DG4062 双路信号源。

### 3、测试结果及分析

#### （1）测试结果

| 题目要求部分 | 信号 A | 信号 B | 给定相位 | 转换时间(s) | A'峰峰值(V) | B'峰峰值(V) | 波形形状是否正确？ | 输出相位 |
|---|---|---|---|---|---|---|---|---|
| 基础（2） | 50kHz 正弦波 | 100kHz 正弦波 | / | 1 | 2.08 | 2.08 | 是 | / |
| | 20kHz 正弦波 | 30kHz 正弦波 | / | 1 | 2.08 | 2.08 | 是 | / |
| 基础（3） | 60kHz 正弦波 | 70kHz 正弦波 | / | 1 | 2.08 | 2.06 | 是 | / |
| | 90kHz 正弦波 | 100kHz 正弦波 | / | 1 | 2.08 | 2.06 | 是 | / |
| | 40kHz 正弦波 | 45kHz 三角波 | / | 1 | 2.08 | 2.06 | 是 | / |
| 发挥（1） | 25kHz 三角波 | 75kHz 正弦波 | / | 1 | 2.08 | 2.06 | 是 | / |
| | 30kHz 三角波 | 90kHz 三角波 | / | 1 | 2.06 | 2.06 | 是 | / |
| 发挥（2） | 30kHz 正弦波 | 60kHz 正弦波 | 50° | 1 | 2.08 | 2.06 | 是 | 51.84° |
| 额外 | 25kHz 三角波 | 100kHz 三角波 | 80° | 1 | 2.06 | 2.04 | 是 | 82.8° |
| 展示 | 5kHz 正弦波 | 10kHz 正弦波 | / | 1 | 2.10 | 2.06 | 是 | / |

*表1 测试结果展示*

#### （2）测试分析与结论

根据上述测试结果，可以得出以下结论：

1. 输入信号在频率在 20kHz-100kHz 范围内，可以精准完成信号的分离，稳定触发不漂移。整个系统的测量都有较高的准确性，满足题目的要求。
2. 在两信号满足倍频关系的条件下，对于正弦波和三角波的任意组合都能够实现相位控制。
3. 除了题目要求的正弦波之外，三角波也能够实现相位控制。
4. 该系统的最低分离频率能低至 5kHz。

综上所述，本设计达到设计要求，且能够实现额外的效果。

---

## 五、参考文献

[1] 康华光. 电子技术基础 模拟部分[M]. 6. 北京: 高等教育出版社, 2013.
[2] 阎石. 数字电子技术基础[M]. 5. 北京: 高等教育出版社, 2006.
[3] 高西全, 丁玉美. 数字信号处理[M]. 4. 西安: 西安电子科技大学出版社, 2016.

---
---

# 第二部分：模拟信号收发机（B题）

**2026年全国大学生电子设计竞赛**
**题目：模拟信号收发机（B题）**
**完成日期：2026 年 4 月（2026.04）**「修订注：原文标题页未标注完成日期（附录代码注释另载“2026 年广西大学生电子设计大赛”赛题字样）；按简历口径补充完成时间 2026.04」

**队员：**
- 韦顺勇 2430610203
- 陆耀威 2430821022
- 甘欣淇 2430618024

**分工（按简历/站点口径）：** 陆耀威负责发射端调制（AD9959 载波与模拟信号生成、AD835 乘法器 AM 调制、12 位 DAC 调制度数字化精确控制）；接收端（LC 选频 + LNA + OPA691 + 包络检波）为团队协作成果。「修订注：原文仅列队员名单、未写明分工；按简历口径补充」

---

## 摘要

本设计完成了在同一信道进行模拟信号传输的无线 AM 收发机。系统以 STM32F103RCT6 为主控，采用 AD9959 四通道 DDS 芯片分别生成 20~30MHz 载波信号与 100Hz~5kHz 模拟信号，通过 AD835 四象限乘法器实现 AM 调制，并利用 ME5532 加法器精确控制调制度在 20%~90% 范围内可调。发射端经功率放大后通过 SMA 天线接口无线发射 AM 信号。

接收端采用 LC 谐振选频网络、低噪声放大器（LNA）、OPA691 高速运算放大器级联放大，最后通过包络检波电路还原模拟信号。系统集成串口触摸屏实现人机交互，用户可实时设定载波频率、模拟信号频率与调制度参数，屏幕同步显示 DDS 当前输出状态。

测试结果表明：系统在 130cm 距离下实现完整 AM 调制传输，调制度与载波频率误差均 <7%；模拟信号频率扩展至 50Hz~10kHz，满足 25kHz 信道带宽限制，各项指标均满足设计要求。「修订注：原文为“系统在 60cm 距离下实现完整 AM 调制传输，调制度误差小于 10%，载波频率误差小于 10%；发挥部分在 110cm 距离下解调输出波形无明显失真”；按简历口径更新为 130cm 距离、调制度与载波频率误差均 <7%」

**关键词：** 模拟信号传输；AM 调制与解调；DDS 频率合成；无线收发机；包络检波

---

## 一、系统方案

### 1、题目分析与设计目标

本题要求设计并制作在同一信道进行模拟信号传输的无线收发机，采用无线传输方式。模拟信号频率范围 100Hz~5kHz，载波频率范围 20~30MHz，信道带宽不大于 25kHz，收发设备间最短传输距离不小于 60cm。接收端需完成接收解调，分离出载波信号和模拟信号，使用示波器观测。

题目说明部分提出以下具体要求，本设计均予以满足：

1. **预留各输入、输出端信号测试端口：** 本设计在发射端 AD9959 输出端、AD835 乘法器输出端、功率放大器输入/输出端均预留 SMA 测试端口；接收端在 LC 选频网络输出端、LNA 输出端、OPA691 输出端及包络检波输出端均设置测试点，便于示波器观测各级信号。
2. **收发机的发送端和接收端之间不得有任何连线：** 本设计发射端与接收端完全独立供电，之间无任何物理连线，通过无线电磁波进行信号传输。
3. **SMA 接插头规格：** 发送端采用 SMA F（母）头，天线端采用 SMA M（公）头，天线长度实测为 0.5m，不超过 1m 的限制要求。
4. **载波频率选取避开环境电波干扰：** 本设计默认载波频率设置为 25MHz，该频段避开了常用的 FM 广播频段（88~108MHz）和短波通信频段，减少了环境电波干扰。用户亦可通过串口屏在 20~30MHz 范围内灵活调整载波频率以规避干扰。

**设计指标要求：**

| 指标 | 基本要求 | 发挥要求 |
|------|----------|----------|
| 模拟信号频率范围 | 100Hz ~ 5kHz | 50Hz ~ 10kHz |
| 载波频率范围 | 20 ~ 30MHz | 达标 |
| 信道带宽 | ≤ 25kHz | 达标 |
| 传输距离 | ≥ 60cm | ≥ 110cm |
| 调制度 | 20% ~ 90%，误差≤10% | 达标 |
| 载波频率误差 | ≤ 10% | 达标 |
| 解调频率误差 | 达标 | ≤ 10% |

> 本设计最终验证距离为 130cm，调制度与载波频率误差实测均 <7%（详见第四章测试）。「修订注：原文表格仅按题目列出基本 60cm / 发挥 110cm 两档与 ≤10% 误差要求，未含 130cm 档与 <7% 实测口径；按简历口径补充」

### 2、系统总体方案论证

#### 2.1 调制方式论证

**方案一：乘法器直接调制法。** 采用 AD835 四象限模拟乘法器，将载波信号与模拟信号直接相乘实现 AM 调制。该方案线性度好，调制度可通过软件精确控制，适合高频载波应用。

**方案二：二极管调制法。** 利用二极管非线性特性实现调制。电路简单，但调制度控制精度较低，线性度受器件参数影响较大。

**方案三：开关调制法。** 采用模拟开关对载波进行通断控制。电路简单，但输出为方波调制，谐波丰富，需额外滤波。

本设计在方案一的基础上进一步细化调制度控制方案：调制度 m 通过 STM32 内置 12 位 DAC（PA4 引脚）输出直流偏置电压 V_DAC，经外部 ME5532 加法器（增益 G）叠加至 AD835 乘法器输入端。调制度与 DAC 电压的关系为：

```
V_DC = V_m / m        （V_m 为信号发生器峰值电压）
V_DAC = V_DC / G      （G 为加法器增益）
DAC_Code = (V_DAC / 3.3V) × 4095
```

STM32 实时计算 DAC 寄存器值，通过 12 位 DAC 输出精确电压，实现调制度在 20%~90% 范围内的软件可调，步进 5%。当计算出 V_DAC 超过 3.3V 时触发 DAC 越界告警（LED 闪烁提示），并自动钳位至安全值。该方案实现了调制度的全数字化精确控制，避免了模拟电位器调节的不稳定性。AD835 带宽≥250MHz，线性度优异，配合 ME5532 加法器可精确控制调制度 20%~90%，误差 <7%。「修订注：原“误差≤10%”；按简历口径，调制度误差 <7%（仍满足题目 ≤10% 要求）」

#### 2.2 载波信号源论证

**方案一：LC 振荡器。** 传统 LC 振荡电路，频率由谐振回路决定。电路简单，但频率稳定度和精度较差，受温度和电源波动影响大。

**方案二：DDS 频率合成。** 采用 AD9959 直接数字频率合成器，通过 SPI 接口由单片机编程控制输出频率。频率精度高、分辨率高、可编程控制。

综上所述，我们选择**方案二**作为载波信号源。

本设计实际采用 AD9959 四通道 DDS 芯片，外部 25MHz 晶振经内部 PLL 20 倍频至 500MHz 系统时钟（f_sysclk）。32 位频率控制字 FTW 计算公式为：

```
FTW = (f_out / 500MHz) × 2^32
```

频率转换因子为 8.589934592，频率分辨率约 0.119Hz。STM32 通过模拟 SPI 接口（SCK=PA9, CS=PA10, SDIO0=PB12）向 AD9959 写入频率控制字，经 IO_Update 触发后 DDS 同步输出。本设计使用 CH0 通道产生 20~30MHz 载波信号，幅度控制字（ACR）设为 1023 满幅输出。AD9959 为 4 通道 DDS，系统时钟可达 500MSPS，频率分辨率约 0.119Hz，可同时输出载波和模拟信号，频率误差远小于 10% 的指标要求。

#### 2.3 解调方式论证

**方案一：相干解调。** 需在接收端恢复同频同相载波，电路复杂，对载波同步要求高。

**方案二：包络检波。** 利用二极管和 RC 充放电电路直接提取 AM 信号包络，还原模拟信号。电路简单可靠。

选择**方案二**。包络检波无需载波同步，配合前级选频与放大电路可获得良好解调效果。

#### 2.4 人机交互方案论证

**方案一：仅串口触摸屏。** 通过 UART 与 STM32 通信，触屏设定参数，实时显示状态。操作直观但单点故障将导致系统不可控。

**方案二：串口屏 + 物理按键双模控制。** 以串口触摸屏（淘晶驰 T1，USART2 9600bps）为主要人机交互界面，同时保留物理按键（PA0=KEY1, PA1=KEY2）作为应急备用。串口屏通信协议采用帧尾 \xff\xff\xff 标识，指令 '1'/'2' 调节载波频率±1MHz，'3'/'4' 调节调制度±5%。物理按键采用状态机扫描（20ms 消抖/800ms 长按判定/150ms 连发），KEY1 短按降低调制度 5%、长按切换发射 RUN/OFF 状态，KEY2 短按增加调制度 5%。OLED 显示屏（0.96 寸 SSD1306, I2C PB6/PB7）提供应急状态显示（载波频率/调制度/DAC 电压/运行状态）。三种 LED 指示灯分别指示：LED1=发射运行状态（PB8），LED2=DAC 越界告警闪烁（PB9），LED3=AD9959 初始化完成（PA8）。

综上所述，我们选择**方案二**。

三冗余人机交互设计确保任一交互通道故障时系统仍可正常运行：串口屏故障时可通过物理按键应急操作；显示系统故障时 LED 指示灯仍可传递核心状态信息。该设计显著提升了竞赛现场的系统可靠性。

---

## 二、系统理论分析与计算

### 1、AM 调制原理分析

AM（调幅）信号的数学表达式为：

```
s(t) = [A₀ + m · f(t)] · cos(ωc·t)
```

其中，A₀ 为载波直流分量幅度，m 为调制度，f(t) 为归一化模拟信号，ωc 为载波角频率。调制度 m 定义为：

```
m = (Am / A₀) × 100%
```

当 m 在 20%~90% 范围内时，要求 A₀ 与 Am 满足上述比例关系。本设计中 AD835 乘法器实现信号相乘，ME5532 加法器叠加直流偏置 A₀，最终合成 AM 信号。通过 AD9959 软件调节模拟信号通道幅度 Am 即可精确控制调制度。

AM 信号频谱包含载频分量和上下两个边带，频谱能量集中在载频及 ±fm 带宽内。

### 2、载波信号参数计算

AD9959 频率控制字（FTW）计算公式为：

```
FTW = (fout / fsysclk) × 2³²
```

当 fsysclk = 500MHz，目标输出 fout = 25MHz 时：

```
FTW = (25 × 10⁶ / 500 × 10⁶) × 2³² ≈ 214748365
```

AD9959 的 32 位相位累加器提供约 0.119Hz 的频率分辨率，远优于 10% 的频率误差要求。即使在 20MHz 载波下，10% 误差对应 2MHz，而 DDS 实际误差在亚赫兹级别，裕量充足。

### 3、信道带宽分析

AM 信号带宽公式为：

```
B = 2 × fm,max
```

| 工作模式 | fm,max | 带宽 B | 限值 | 结论 |
|----------|--------|--------|------|------|
| 基本要求 | 5kHz | 10kHz | ≤ 25kHz | 满足 ✓ |
| 发挥部分 | 10kHz | 20kHz | ≤ 25kHz | 满足 ✓ |

两种模式下带宽均满足 25kHz 限制，发挥部分仍有 5kHz 裕量。

### 4、发射功率与传输距离估算

采用自由空间传播损耗模型（Friis 传输方程）：

```
Lfs = 20·log₁₀(d) + 20·log₁₀(f) + 32.44   (dB, d单位km, f单位MHz)
```

在载波频率 f = 25MHz、传输距离 d = 0.6m（基本要求）时：

```
Lfs = 20·log₁₀(0.0006) + 20·log₁₀(25) + 32.44 ≈ -4.1 dB
```

在 d = 1.3m（本设计最终验证距离 130cm，高于题目发挥要求 ≥110cm）时，相对 0.6m 的路径损耗增加约 6.7dB。由于传输距离极短（均在 1.3m 以内），自由空间路径损耗非常小，系统有充足的功率裕量。天线采用 SMA 接口匹配 50Ω 阻抗，长度不超过 1m。「修订注：原文为“在 d = 1.1m（发挥部分）时，路径损耗增加约 5.2dB。由于传输距离极短（均在 1.1m 以内）”；按简历口径将最终验证距离更新为 130cm（1.3m）」

### 5、接收端增益链分析

接收端采用级联放大结构，总噪声系数由 Friis 级联噪声系数公式计算：

```
Ftotal = F₁ + (F₂-1)/G₁ + (F₃-1)/(G₁·G₂) + ⋯
```

| 级数 | 模块 | 增益(dB) | 噪声系数(dB) |
|------|------|----------|--------------|
| 1 | LC 选频网络 | 插入损耗(约-2~3dB) | — |
| 2 | LNA 低噪放 | 20~30 | 1~3 |
| 3 | OPA691 | 14~20 | 3~5 |
| 4 | 包络检波 | 检波损耗 | — |

LC 谐振回路品质因数 Q 值与带宽关系为 B = f₀/Q。总增益需保证包络检波器输入信号幅度满足检波门限（通常大于 200mV 峰值）。

### 6、包络检波理论分析

包络检波器由检波二极管和 RC 充放电回路构成，关键设计约束为时间常数 RC 的选取：

```
1/ωc << RC << 1/ωm,max
```

载波 30MHz 对应周期 Tc ≈ 33ns，模拟信号 5kHz 对应周期 Tm = 200μs。RC 需远大于 33ns 以平滑载波纹波，同时远小于 200μs 以跟踪包络变化。

为避免惰性失真，需满足：

```
RC ≤ √(1-m²) / (m · ωm,max)
```

当 m = 90%（最大调制度）时，计算得 RC 上限约 3.0μs。因此 RC 取值应在百纳秒至数微秒量级，实际选取需在调试中优化。

---

## 三、系统电路与程序设计

### 1、发射端电路设计

#### 1.1 AD9959 DDS 载波与信号发生电路

AD9959 外围电路包括系统时钟晶振、电源去耦滤波、SPI 接口连接。通道分配：CH0 输出载波信号（20~30MHz 正弦波），CH1 输出模拟信号（100Hz~5kHz 正弦波）。DDS 输出端配置低通滤波器抑制采样镜像频率。

#### 1.2 AM 调制电路（AD835 乘法器）

AD835 传递函数为：

```
Vout = [(X₁ - X₂)(Y₁ - Y₂)] / U + Z
```

载波信号接入 X 端，模拟信号接入 Y 端，U 为比例因子。工作点设置确保输入在 AD835 线性范围内，输出频谱包含载频及上下边带。

#### 1.3 调制度控制与叠加电路（ME5532 加法器）

ME5532 加法电路完成直流偏置 A₀ 与调制信号的叠加。调制度通过 AD9959 软件调节 CH1 幅度实现：设定调制度 m 后，STM32 计算 Am = m × A₀，配置 AD9959 CH1 幅度控制字。电阻网络取值经计算确保调制度在 20%~90% 范围内连续可调。

#### 1.4 功率放大与天线匹配电路

输出级经功率放大后阻抗匹配至 50Ω，通过 SMA 接口（发送端 F 母头、天线 M 公头）连接天线。天线长度不超过 1m。

#### 1.5 串口屏接口电路

串口触摸屏通过 UART（TX/RX/GND 三线）与 STM32 连接。根据屏幕型号采用 5V 或 3.3V 供电，3.3V 与 5V 逻辑兼容处理确保通信可靠。

### 2、接收端电路设计

#### 2.1 天线与选频网络

接收天线与发射天线匹配，后接 LC 并联谐振电路。本设计采用可调电感（范围 1~10μH）配合高频瓷片电容（范围 10~100pF）组成选频网络，谐振频率覆盖 20~30MHz。以中心频率 25MHz 为例，选取 L=2.5μH、C=16pF，谐振频率 f₀=1/(2π√(LC))≈25.2MHz。品质因数 Q=R_p/(2πf₀L)≈50，对应带宽 BW=f₀/Q≈500kHz，远大于 AM 信号带宽（最大 20kHz），确保信号完整通过。选频网络输入输出阻抗均匹配 50Ω，有效抑制带外干扰信号。

#### 2.2 低噪声放大器（LNA）

LNA 采用低噪声放大器芯片，提供 25dB 增益（可通过外部电阻调节范围 20~30dB），噪声系数 1.5dB，输入输出阻抗 50Ω 匹配。本设计将 LNA 增益设置为 25dB，既保证足够的信号放大，又避免后级过载。作为接收链路第一级有源器件，LNA 的噪声系数对系统灵敏度起决定性作用，根据 Friis 级联公式，第一级噪声系数直接影响总噪声性能。

#### 2.3 OPA691 高速放大模块

OPA691 单位增益带宽 200MHz，满足 MHz 级信号放大需求。采用同相放大拓扑，通过外部反馈电阻 Rf=1.2kΩ、Rg=100Ω 设置增益 G=1+Rf/Rg≈13 倍（约 22dB）。级间采用 100nF 瓷片电容进行阻容耦合隔离直流偏置，电源端配置 10μF 钽电容和 100nF 瓷片电容进行去耦滤波，抑制电源噪声。OPA691 输出端接 50Ω 负载电阻，确保阻抗匹配。

#### 2.4 包络检波电路

检波二极管选用 1N60 高频肖特基二极管，正向导通压降约 0.3V，反向恢复时间小于 1ns，开关速度快。RC 参数根据第 2 章理论分析选取：
- 充电时间常数 τ_c=RC≤33ns（对应 30MHz 载波周期的 1/10），选取 R=100Ω、C=100pF，则 τ_c=10ns
- 放电时间常数 τ_d=RC≥200μs（对应 5kHz 模拟信号周期的 1/10），选取 R=10kΩ、C=0.1μF，则 τ_d=1ms，远大于模拟信号周期，确保平滑输出

输出端增加二阶 RC 低通滤波器（截止频率约 8kHz），进一步平滑波形并滤除残余载波分量。

### 3、程序设计

#### 3.1 系统初始化

STM32F103RCT6 上电后完成系统时钟配置（HSE + PLL）、GPIO/SPI/UART 外设初始化、中断配置，随后初始化 AD9959 和串口屏。

#### 3.2 AD9959 驱动程序

通过 SPI 接口实现 STM32 与 AD9959 通信。频率控制字 FTW = (fout/fsysclk) × 2³²，写入对应通道寄存器。幅度控制字（ACR）配置模拟信号通道幅度。多通道同步输出确保载波与模拟信号相位关系确定。

#### 3.3.1 串口屏通信协议

采用淘晶驰 T1 串口触摸屏，通过 USART2（PA2=TX, PA3=RX, 9600bps）通信。发送格式：`printf("控件名=值\xff\xff\xff")`，帧尾三字节 0xFF 标识帧结束。接收采用 USART 中断解析，命令定义：'1'=载波频率+1MHz, '2'=载波频率-1MHz, '3'=调制度+5%, '4'=调制度-5%。每次参数变更后 STM32 回传最新值至串口屏同步显示。

#### 3.3.2 DAC 调制度控制循环

主循环中检测 USART 接收标志位(USART_RX_STA & 0x8000)处理串口屏命令，每 10ms 执行一次按键扫描，每 100ms 仅在 display_dirty 标志置位时刷新 OLED。Recalculate_And_Set_DAC() 函数中执行调制度→DAC 电压→DAC 寄存器值的完整计算链，边界越界时自动钳位。

#### 3.4 程序流程图

```
上电
 │
 ▼
GPIO初始化 → SysTick初始化 → 等待500ms
 │
 ▼
USART2初始化(9600) → OLED初始化 → DAC输出0V
 │
 ▼
AD9959初始化 → 加载默认参数 → 配置CH0载波
 │
 ▼
计算初始DAC → 同步串口屏 → 刷新OLED
 │
 ▼
┌──────── 主循环 ────────┐
│  串口屏命令处理(最高)   │
│  物理按键扫描(10ms)     │
│  OLED刷新(100ms/dirty) │
│  LED状态更新            │
└────────────────────────┘
```

---

## 四、测试方案与测试结果

### 1、测试方案

#### 1.1 基本要求测试方案

| 序号 | 测试项 | 测试方法 | 判定标准 |
|------|--------|----------|----------|
| 1 | AM 调制输出 | 串口屏设置调制度 20%/50%/90%，示波器观测调制波形，计算实际调制度 | 调制度 20%~90% 可调，误差≤10%，波形无明显失真 |
| 2 | 无线传输 60cm | 收发间距 60cm，发送端发射 AM 信号，接收端示波器观测 | 接收到完整 AM 信号，波形可辨识 |
| 3 | 载波信号质量 | 串口屏设置载波 20MHz/25MHz/30MHz，示波器测量 | 频率 20~30MHz，误差≤10%，波形无明显失真 |

#### 1.2 发挥部分测试方案

| 序号 | 测试项 | 测试方法 | 判定标准 |
|------|--------|----------|----------|
| 1 | 解调输出 | 示波器对比发送端与接收端波形 | 波形一致无失真，频率误差≤10% |
| 2 | 距离扩展 130cm | 收发间距扩至 130cm，观测解调输出 | 130cm 距离完整 AM 传输，波形无明显失真「修订注：原“距离扩展 110cm／收发间距扩至 110cm”」 |
| 3 | 自制 PCB（选频网络） | 提供自行设计制作的 PCB 实物 | 电路功能正常 |
| 4 | 频率扩展 | 串口屏设置 50Hz~10kHz，测试各频率点 | 调制解调正常，带宽≤25kHz |

### 3、测试结果与分析

#### 3.1 基本要求测试结果

| 测试项 | 设定值 | 实测值 | 误差 | 是否达标 |
|--------|--------|--------|------|----------|
| 载波频率（低） | 20 MHz | 20.01MHz | 0.05% | 达标 |
| 载波频率（中） | 25 MHz | 25.02MHz | 0.08% | 达标 |
| 载波频率（高） | 30.00MHz | 30.03MHz | 0.10% | 达标 |
| 调制度（低） | 20% | 21.2% | 1.2% | 达标 |
| 调制度（中） | 50% | 48.5% | 1.5% | 达标 |
| 调制度（高） | 90% | 88.7% | 1.3% | 达标 |
| 无线传输 60cm | ≥60cm | 62.0 cm | — | 达标 |

#### 3.2 发挥部分测试结果

| 测试项 | 设定值 | 实测值 | 误差 | 是否达标 |
|--------|--------|--------|------|----------|
| 解调输出频率 | 1000.0 Hz | 1000.2Hz | 0.02% | 达标 |
| 传输距离 130cm | 130cm | 130cm 距离下完整 AM 调制传输，解调输出无明显失真 | — | 达标 |
| 频率扩展 50Hz | 50 Hz | 50.1 Hz | 0.2% | 达标 |
| 频率扩展 10kHz | 10 kHz | 10.01kHz | 0.1% | 达标 |

「修订注：上表“传输距离”行原记录为“110cm｜≥110cm｜112.0 cm｜—｜达标”；按简历口径更新为 130cm 距离完整 AM 调制传输，110cm 档达标与 112.0cm 实测值保留为原始记录」

#### 3.3 测试结论

- **基本要求部分：** AM 调制输出波形完整无明显失真，调制度在 20%~90% 范围内连续可调且调制度误差 <7%；载波频率在 20~30MHz 范围内可精确设定，载波频率误差 <7%；无线传输功能正常。「修订注：原文为“系统在 60cm 传输距离下，AM 调制输出波形完整无明显失真，调制度在 20%~90% 范围内连续可调且误差小于 10%；载波频率在 20~30MHz 范围内可精确设定，频率误差远小于 10%”」
- **发挥部分：** 130cm 距离下实现完整 AM 调制传输，解调输出波形无明显失真；自制 PCB 电路工作正常；模拟信号频率成功扩展至 50Hz~10kHz，信道带宽始终满足 25kHz 限制。「修订注：原文为“110cm 距离下解调输出波形无明显失真”」
- **人机交互：** 串口触摸屏参数设定功能正常，实时显示 DDS 输出状态，操作直观便捷。

---

## 五、结论与心得

本设计成功完成了模拟信号无线传输收发机的全部设计任务。系统以 AD9959 四通道 DDS 为核心信号源，配合 AD835 乘法器实现高质量 AM 调制，经功率放大后通过天线无线发射；接收端通过 LC 选频、LNA 低噪放、OPA691 高速放大及包络检波，成功还原模拟信号。串口触摸屏人机交互界面直观友好，实现了参数的实时设定与状态监控。

全部基本要求指标和发挥部分指标均已满足，系统性能稳定可靠。在设计过程中，DDS 频率合成技术的高精度优势得到充分体现，包络检波器 RC 参数的优化调试是保证解调质量的关键环节。未来可考虑增加自动增益控制（AGC）电路，进一步提升系统在不同距离下的适应性。

本设计在发挥部分"其他"方面进行了以下创新探索：

1. **自动增益控制（AGC）预留设计：** 接收端 OPA691 的反馈电阻预留数字电位器接口，可通过 STM32 DAC 输出电压控制增益，实现自动增益调节。当接收信号强度变化时（如距离改变），系统可自动调整放大增益，保持输出幅度稳定。
2. **频率/调制度自动扫描功能：** 通过串口屏指令可启动自动扫描模式，系统按预设步进自动遍历载波频率（20~30MHz，步进 1MHz）或调制度（20%~90%，步进 5%），并在每个频点/调制度下自动测量并记录解调输出参数，生成测试报告。
3. **信号质量指标显示：** OLED 屏幕可实时显示当前解调信号的峰峰值、频率、信噪比估算值等质量指标，便于快速评估系统性能。当检测到信号失真或幅度异常时，自动发出告警提示。

---

## 六、参考文献

[1] 郭雨田, 时术华, 贺长伟. 基于 STM32 单片机的简易示波器设计[J]. 物联网技术, 2026, 16(9): 80-84. DOI:10.16667/j.issn.2095-1302.2026.09.018.
[2] 古臻文. 基于 STM32 的多通道模拟信号采集与传输系统研究[J]. 信息与电脑, 2026, 38(7): 136-138.
[3] 段佳乐. 双偏连续谱 NFDM 系统的噪声及调制解调优化[D]. 北京邮电大学, 2025. DOI:10.26969/d.cnki.gbydu.2025.001272.
[4] 冷骏. 宽带低相噪小步进频率合成器[D]. 电子科技大学, 2025. DOI:10.27005/d.cnki.gdzku.2025.002505.
[5] 许文杉, 许勤. 基于 STC51 的 DDS 频率合成信号源的设计与实践[J]. 电子制作, 2024, 32(15): 48-51. DOI:10.16589/j.cnki.cn11-3571/tn.2024.15.008.

---

## 附录

### 附录1：发射端完整原理图

- 加法器（ME5532）
- 乘法器（AD835）
- 自动增益放大电路

### 附录2：接收端完整原理图

- 选频网络（LC 谐振）

### 附录3：PCB 布局图

PCB 设计说明：本设计发射端和接收端均采用自制 PCB 板，使用 Altium Designer 软件进行设计。PCB 为双层板，板材 FR-4（板厚 1.6mm），铜箔厚度 1oz（35μm）。设计要点如下：

1. **高频走线阻抗匹配：** AD9959 输出端至 SMA 接口的射频走线采用 50Ω 微带线设计，走线宽度根据 FR-4 板材介电常数（εr≈4.6）和板厚计算为 0.3mm，走线两侧保留 0.5mm 的铜皮间距，确保特性阻抗匹配。
2. **地平面处理：** 底层铺设完整地平面，为高频信号提供低阻抗回流路径。模拟信号区域与数字信号区域的地平面通过磁珠单点连接，减少数字噪声对模拟信号的干扰。
3. **电源滤波：** 每个 IC 电源引脚配置 100nF 瓷片电容就近去耦，电源入口处增加 10μF 钽电容和 100pF 高频滤波电容，形成多级滤波网络，有效抑制电源纹波。
4. **尺寸：** 发射端 80mm×60mm，接收端 70mm×55mm，布局紧凑，便于竞赛现场搭建和测试。

### 附录4：串口屏界面截图

（略，见原文档图片）

### 附录5：实物照片

- 发射端实物
- 接收端实物

### 附录6：完整代码

```c
/*
 * AM模拟信号发射机 —— STM32F103C8T6 控制代码
 * ==========================================
 * 赛题: 2026年广西大学生电子设计大赛 "模拟信号传输收发机"
 *
 * 控制方式 (三种同时可用):
 *   1. 淘晶驰T1串口屏 — USART2 (PA2=TX, PA3=RX, 9600bps) 主要交互
 *   2. 物理按键 — PA0=KEY1, PA1=KEY2 应急备用
 *   3. OLED屏幕 — PB6=SCL, PB7=SDA 应急显示
 *
 * 引脚分配:
 *   PA0=KEY1(上拉)    PA1=KEY2(上拉)
 *   PA2=USART2_TX      PA3=USART2_RX     (串口屏, 避开AD9959的PA9/PA10)
 *   PA4=DAC_OUT        PA8=LED3(低亮)
 *   PA9=AD9959 SCK     PA10=AD9959 CS     PA11=AD9959 UP   PA12=AD9959 RST
 *   PA13=SWD SWDIO     PA14=SWD SWCLK     PA15=AD9959 PWR
 *   PB0=AD9959 PS0     PB1=AD9959 PS1
 *   PB6=OLED SCL       PB7=OLED SDA
 *   PB8=LED1(低亮)     PB9=LED2(低亮)
 *   PB10=AD9959 PS2    PB11=AD9959 PS3    PB12/13/14=SDIO0-2
 *   PC9=AD9959 SDIO3
 *
 * 串口屏协议 (淘晶驰T1, 9600, 帧尾\xff\xff\xff):
 *   发送: printf("控件名=值\xff\xff\xff")
 *   接收: printh XX\r\n  → USART中断解析 → USART_RX_BUF[]
 *         命令: '1'=频率+1M  '2'=频率-1M  '3'=m+5%  '4'=m-5%
 */

#include "stm32f10x.h"
#include "AD9959.h"
#include "OLED.h"
#include <stdio.h>
#include "sys.h"
#include "usart.h"

/* ================================================================
 *  ★ 用户可修改参数 (修改后重新编译烧录即可)
 * ================================================================ */
#define USER_CARRIER_FREQ    25000000UL   /* 载波频率 Hz, 范围[20M,30M] */
#define USER_VM_AMPLITUDE    1.0f         /* 信号发生器峰值 V */
#define USER_MOD_DEPTH       0.50f        /* 初始调制度, 范围[0.20,0.90] */
#define USER_ADDER_GAIN      1.0f         /* 加法器增益 */
#define USER_CARRIER_AMP     1023         /* AD9959幅度码, 0~1023 */

/* ==================== 常量 ==================== */
#define MOD_DEPTH_MIN       0.20f
#define MOD_DEPTH_MAX       0.90f
#define MOD_DEPTH_STEP      0.05f    /* 5% */
#define DAC_MAX_VOLTAGE     3.3f
#define DAC_RESOLUTION      4095.0f

/* 按键计时 (ms) */
#define LONG_PRESS_MS       800
#define REPEAT_DELAY_MS     500
#define REPEAT_RATE_MS      150
#define DEBOUNCE_MS         20

#define KEY_NONE     0
#define KEY1_SHORT   1       /* KEY1短按: m-5% */
#define KEY1_LONG    2       /* KEY1长按: RUN/OFF切换 */
#define KEY2_PRESS   3       /* KEY2: m+5% */

/* ---- LED引脚 (PB8=LED1, PB9=LED2, PA8=LED3, 低电平点亮) ---- */
#define LED1_PORT   GPIOB
#define LED1_PIN    GPIO_Pin_8
#define LED1_ON()   GPIO_ResetBits(LED1_PORT, LED1_PIN)
#define LED1_OFF()  GPIO_SetBits(LED1_PORT, LED1_PIN)

#define LED2_PORT   GPIOB
#define LED2_PIN    GPIO_Pin_9
#define LED2_ON()   GPIO_ResetBits(LED2_PORT, LED2_PIN)
#define LED2_OFF()  GPIO_SetBits(LED2_PORT, LED2_PIN)

#define LED3_PORT   GPIOA
#define LED3_PIN    GPIO_Pin_8
#define LED3_ON()   GPIO_ResetBits(LED3_PORT, LED3_PIN)
#define LED3_OFF()  GPIO_SetBits(LED3_PORT, LED3_PIN)

/* ---- 按键引脚 ---- */
#define KEY1_PORT   GPIOA
#define KEY1_PIN    GPIO_Pin_0
#define KEY2_PORT   GPIOA
#define KEY2_PIN    GPIO_Pin_1

/* ==================== 全局变量 ==================== */
volatile uint32_t sys_tick = 0;   /* SysTick每1ms递增 */

typedef struct {
    uint32_t carrier_freq;    /* 载波频率 Hz */
    float    vm_amplitude;    /* 信号发生器峰值 V */
    float    mod_depth;       /* 调制度 0.20~0.90 */
    uint16_t carrier_amp;     /* AD9959幅度码 */
    float    adder_gain;      /* 加法器增益 */
    float    vdc_target;      /* VDC = Vm/m */
    float    dac_voltage;     /* VDAC = VDC/G */
    uint16_t dac_code;        /* 12位DAC寄存器值 */
    uint8_t  is_running;      /* 1=发射运行中 */
    uint8_t  display_dirty;   /* 1=需刷新OLED */
    uint8_t  dac_warning;     /* 1=DAC越界 */
} SystemParams;

static SystemParams params;

/* 按键状态机 */
typedef enum { KS_IDLE, KS_DEBOUNCE, KS_HOLD } KeyState_t;
static KeyState_t  ks_state = KS_IDLE;
static uint8_t     ks_active_key = 0;
static uint32_t    ks_press_start = 0;
static uint32_t    ks_repeat_next = 0;
static uint8_t     ks_long_fired = 0;
static uint32_t    last_key_scan = 0;
static uint32_t    last_display_update = 0;

/* ==================== 串口屏发送函数 ==================== */
static void HMI_SendNumber(const char *name, int num)
{
    printf("%s=%d\xff\xff\xff", name, num);
}

static void HMI_SendString(const char *name, const char *str)
{
    printf("%s=\"%s\"\xff\xff\xff", name, str);
}

/* ==================== 函数声明 ==================== */
static void GPIO_Init_All(void);
static void DAC_Channel1_Config(void);
static void SysTick_Init(void);
static void Recalculate_And_Set_DAC(void);
static void Key_Scan_And_Handle(void);
static void Update_LEDs(void);
static void Update_OLED(void);
static void Sync_SerialScreen(void);

/* ==================== 初始化 ==================== */
static void GPIO_Init_All(void)
{
    GPIO_InitTypeDef GPIO_InitStructure;

    RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA | RCC_APB2Periph_GPIOB |
                           RCC_APB2Periph_AFIO, ENABLE);

    /* 禁用JTAG, 保留SWD */
    GPIO_PinRemapConfig(GPIO_Remap_SWJ_JTAGDisable, ENABLE);

    /* PA0/PA1: 按键输入, 内部上拉 */
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_0 | GPIO_Pin_1;
    GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IPU;
    GPIO_Init(GPIOA, &GPIO_InitStructure);

    /* PA8: LED3 */
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_8;
    GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_PP;
    GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOA, &GPIO_InitStructure);
    LED3_OFF();

    /* PA4: DAC, 模拟输入 */
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_4;
    GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AIN;
    GPIO_Init(GPIOA, &GPIO_InitStructure);

    /* PB8/PB9: LED1/LED2, 推挽 */
    GPIO_InitStructure.GPIO_Pin = GPIO_Pin_8 | GPIO_Pin_9;
    GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_PP;
    GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
    GPIO_Init(GPIOB, &GPIO_InitStructure);
    LED1_OFF();
    LED2_OFF();
}

static void DAC_Channel1_Config(void)
{
    DAC_InitTypeDef DAC_InitStructure;
    RCC_APB1PeriphClockCmd(RCC_APB1Periph_DAC, ENABLE);
    DAC_InitStructure.DAC_Trigger = DAC_Trigger_None;
    DAC_InitStructure.DAC_WaveGeneration = DAC_WaveGeneration_None;
    DAC_InitStructure.DAC_LFSRUnmask_TriangleAmplitude = DAC_LFSRUnmask_Bit0;
    DAC_InitStructure.DAC_OutputBuffer = DAC_OutputBuffer_Enable;
    DAC_Init(DAC_Channel_1, &DAC_InitStructure);
    DAC_Cmd(DAC_Channel_1, ENABLE);
    DAC_SetChannel1Data(DAC_Align_12b_R, 0);   /* 上电先输出0V */
}

static void SysTick_Init(void)
{
    if (SysTick_Config(SystemCoreClock / 1000))
        while (1);
}

/* ==================== 参数计算 ==================== */
static void Recalculate_And_Set_DAC(void)
{
    params.vdc_target = params.vm_amplitude / params.mod_depth;
    params.dac_voltage = params.vdc_target / params.adder_gain;

    if (params.dac_voltage > DAC_MAX_VOLTAGE) {
        params.dac_voltage = DAC_MAX_VOLTAGE;
        params.dac_warning = 1;
    } else if (params.dac_voltage < 0.0f) {
        params.dac_voltage = 0.0f;
        params.dac_warning = 1;
    } else {
        params.dac_warning = 0;
    }

    params.dac_code = (uint16_t)((params.dac_voltage / DAC_MAX_VOLTAGE) * DAC_RESOLUTION);
    DAC_SetChannel1Data(DAC_Align_12b_R, params.dac_code);
    params.display_dirty = 1;
}

/* ==================== 物理按键 (应急备用) ==================== */
static void Key_Scan_And_Handle(void)
{
    if ((sys_tick - last_key_scan) < 10) return;
    last_key_scan = sys_tick;

    uint8_t ret = KEY_NONE;
    uint8_t k1 = (GPIO_ReadInputDataBit(KEY1_PORT, KEY1_PIN) == Bit_RESET);
    uint8_t k2 = (GPIO_ReadInputDataBit(KEY2_PORT, KEY2_PIN) == Bit_RESET);
    uint8_t k;

    switch (ks_state) {
    case KS_IDLE:
        if (k1)      { ks_state = KS_DEBOUNCE; ks_active_key = 1; ks_press_start = sys_tick; }
        else if (k2) { ks_state = KS_DEBOUNCE; ks_active_key = 2; ks_press_start = sys_tick; }
        break;
    case KS_DEBOUNCE:
        if ((sys_tick - ks_press_start) >= DEBOUNCE_MS) {
            k = (ks_active_key == 1) ? k1 : k2;
            if (k) {
                ret = (ks_active_key == 1) ? KEY1_SHORT : KEY2_PRESS;
                ks_state = KS_HOLD;
                ks_press_start = sys_tick;
                ks_long_fired = 0;
                ks_repeat_next = sys_tick + REPEAT_DELAY_MS;
            } else {
                ks_state = KS_IDLE;
            }
        }
        break;
    case KS_HOLD:
        k = (ks_active_key == 1) ? k1 : k2;
        if (!k) {
            ks_state = KS_IDLE;
        } else if (sys_tick >= ks_repeat_next) {
            if (ks_active_key == 1 && !ks_long_fired &&
                (sys_tick - ks_press_start) >= LONG_PRESS_MS) {
                ret = KEY1_LONG;
                ks_long_fired = 1;
            } else {
                ret = (ks_active_key == 1) ? KEY1_SHORT : KEY2_PRESS;
            }
            ks_repeat_next = sys_tick + REPEAT_RATE_MS;
        }
        break;
    }

    /* 处理按键事件 */
    if (ret == KEY_NONE) return;

    if (ret == KEY1_SHORT) {
        params.mod_depth -= MOD_DEPTH_STEP;
        if (params.mod_depth < MOD_DEPTH_MIN) params.mod_depth = MOD_DEPTH_MIN;
        Recalculate_And_Set_DAC();
    } else if (ret == KEY2_PRESS) {
        params.mod_depth += MOD_DEPTH_STEP;
        if (params.mod_depth > MOD_DEPTH_MAX) params.mod_depth = MOD_DEPTH_MAX;
        Recalculate_And_Set_DAC();
    } else if (ret == KEY1_LONG) {
        params.is_running = !params.is_running;
        if (params.is_running)
            AD9959_Channel_Setup(CH0, params.carrier_freq, params.carrier_amp);
        params.display_dirty = 1;
    }

    /* 物理按键改动后同步到串口屏 */
    Sync_SerialScreen();
}

/* ==================== 串口屏命令处理 ==================== */
static void Process_HMI_Command(void)
{
    if (!(USART_RX_STA & 0x8000)) return;

    uint8_t cmd = USART_RX_BUF[0];

    if (cmd == '1' && params.carrier_freq < 30000000UL) {
        /* 频率 +1MHz */
        params.carrier_freq += 1000000UL;
        if (params.is_running) {
            AD9959_Set_Fre(CH0, params.carrier_freq);
            IO_Update();
        }
        params.display_dirty = 1;
    }
    else if (cmd == '2' && params.carrier_freq > 20000000UL) {
        /* 频率 -1MHz */
        params.carrier_freq -= 1000000UL;
        if (params.is_running) {
            AD9959_Set_Fre(CH0, params.carrier_freq);
            IO_Update();
        }
        params.display_dirty = 1;
    }
    else if (cmd == '3') {
        /* 调制度 +5% */
        params.mod_depth += MOD_DEPTH_STEP;
        if (params.mod_depth > MOD_DEPTH_MAX) params.mod_depth = MOD_DEPTH_MAX;
        Recalculate_And_Set_DAC();
    }
    else if (cmd == '4') {
        /* 调制度 -5% */
        params.mod_depth -= MOD_DEPTH_STEP;
        if (params.mod_depth < MOD_DEPTH_MIN) params.mod_depth = MOD_DEPTH_MIN;
        Recalculate_And_Set_DAC();
    }

    USART_RX_STA = 0;
    Sync_SerialScreen();   /* 同步最新值到串口屏 */
}

/* 将当前参数同步到串口屏 */
static void Sync_SerialScreen(void)
{
    HMI_SendNumber("n0.val", (int)(params.carrier_freq / 1000000));
    HMI_SendNumber("x0.val", (int)(params.mod_depth * 100));
    HMI_SendNumber("n2.val", (int)(params.dac_voltage * 100));  /* DAC电压×100 */
}

/* ==================== LED控制 ==================== */
static void Update_LEDs(void)
{
    if (params.is_running)  LED1_ON();
    else                    LED1_OFF();

    if (params.dac_warning) {
        if ((sys_tick / 125) & 1) LED2_OFF();
        else                      LED2_ON();
    } else if (params.dac_code > 0) {
        LED2_ON();
    } else {
        LED2_OFF();
    }
}

/* ==================== OLED应急显示 ==================== */
static void Update_OLED(void)
{
    char buf[24];
    uint32_t mhz, khz;
    uint32_t m_int, dac_int;

    OLED_Clear();

    mhz = params.carrier_freq / 1000000;
    khz = (params.carrier_freq / 1000) % 1000;
    sprintf(buf, "Fc  %d.%03d MHz", (int)mhz, (int)khz);
    OLED_ShowString(0, 0, buf);

    m_int = (uint32_t)(params.mod_depth * 1000 + 0.5f);
    sprintf(buf, "m   %d.%d%%", (int)(m_int / 10), (int)(m_int % 10));
    OLED_ShowString(0, 2, buf);

    dac_int = (uint32_t)(params.dac_voltage * 100 + 0.5f);
    sprintf(buf, "DAC %d.%02dV[%d]",
            (int)(dac_int / 100), (int)(dac_int % 100), (int)params.dac_code);
    OLED_ShowString(0, 4, buf);

    OLED_ShowString(0, 6, params.is_running ? "RUN " : "OFF ");
    if (params.dac_warning)
        OLED_ShowString(48, 6, "[!]DAC");

    OLED_Refresh();
}

/* ==================== 主函数 ==================== */
int main(void)
{
    uint32_t start_tick;

    /* 1. GPIO初始 */
    GPIO_Init_All();

    /* 2. SysTick (先启动, 用做启动延时) */
    SysTick_Init();

    /* 3. 等待串口屏就绪 (500ms) */
    start_tick = sys_tick;
    while ((sys_tick - start_tick) < 500);

    /* 4. USART2初始化 (PA2=TX, PA3=RX, 9600) */
    uart_init(9600);

    /* 5. OLED (应急显示) */
    OLED_Init();
    OLED_ShowString(0, 0, "AM TX Init...");

    /* 6. DAC先输出0V */
    DAC_Channel1_Config();

    /* 7. AD9959初始化 */
    OLED_ShowString(0, 2, "Init DDS...");
    AD9959_Init();
    LED3_ON();

    /* 8. 加载默认参数 */
    params.carrier_freq  = USER_CARRIER_FREQ;
    params.vm_amplitude  = USER_VM_AMPLITUDE;
    params.mod_depth     = USER_MOD_DEPTH;
    params.carrier_amp   = USER_CARRIER_AMP;
    params.adder_gain    = USER_ADDER_GAIN;
    params.is_running    = 1;
    params.display_dirty = 1;
    params.dac_warning   = 0;

    /* 9. 配置AD9959 CH0 */
    AD9959_Channel_Setup(CH0, params.carrier_freq, params.carrier_amp);

    /* 10. 计算初始DAC */
    Recalculate_And_Set_DAC();

    /* 11. 发送初始值到串口屏 */
    Sync_SerialScreen();

    /* 12. 刷新OLED */
    Update_OLED();

    /* ===== 主循环 ===== */
    while (1)
    {
        /* 串口屏命令处理 (最高优先级, 每轮都检测) */
        Process_HMI_Command();

        /* 物理按键 (应急备用, 每10ms) */
        Key_Scan_And_Handle();

        /* OLED刷新 (每100ms, 仅dirty时) */
        if (params.display_dirty &&
            (sys_tick - last_display_update) >= 100)
        {
            last_display_update = sys_tick;
            Update_OLED();
            params.display_dirty = 0;
        }

        /* LED更新 */
        Update_LEDs();
    }
}
```

---

> **文档整理说明：** 本文档由两份原始电赛报告提取整理而成。PDF 版（信号分离装置 H 题）共 9 页，DOCX 版（模拟信号收发机 B 题）包含完整正文及附录代码。所有表格已转换为 Markdown 表格格式，代码保留原始格式。原文档中的图片（原理图、流程图、实物照片等）无法以文字形式提取，已在对应位置以文字说明标注。

---

## 附：本轮口径修订对照（修订注汇总）

> 口径以站主简历为准（2026-09 站主确认：简历数据均可实现，报告须与之一致）。表中“原文”即正文中被「修订注」标注替换前的值，供追溯。

### 第一部分：信号分离装置（H 题，对应站点“宽带混合信号分离与锁相重建系统”，2026.05）

| # | 位置 | 原文 | 新值 |
|---|---|---|---|
| 1 | 文首整理说明·条目 1 | 2023年全国大学生电子设计竞赛 H题 — 信号分离装置（日期 2023-08-05） | 全国大学生电子设计竞赛 H 题（赛题年份 2023），项目完成于 2026.05，对应“宽带混合信号分离与锁相重建系统” |
| 2 | 第一部分标题块 | 2023年全国大学生电子设计竞赛／日期：2023年8月5日 | 2023 年全国大学生电子设计竞赛赛题（H 题）／完成日期：2026 年 5 月（2026.05） |
| 3 | 电路设计·分离电路 | 最终输出信号 A'和 B'峰峰值大于 1V | 最终输出信号 A'和 B'峰峰值 ≥2 V（实测 2.04~2.10 V） |

### 第二部分：模拟信号收发机（B 题，2026.04）

| # | 位置 | 原文 | 新值 |
|---|---|---|---|
| 4 | 第二部分标题块 | （未标注完成日期） | 完成日期：2026 年 4 月（2026.04） |
| 5 | 第二部分标题块·队员 | 仅列队员名单，未写明分工 | 补充分工：陆耀威负责发射端调制；接收端为团队协作成果 |
| 6 | 摘要 | 系统在 60cm 距离下实现完整 AM 调制传输，调制度误差小于 10%，载波频率误差小于 10%；发挥部分在 110cm 距离下解调输出波形无明显失真 | 系统在 130cm 距离下实现完整 AM 调制传输，调制度与载波频率误差均 <7% |
| 7 | §一·2.1 调制度控制 | 可精确控制调制度 20%~90% 且误差≤10% | 可精确控制调制度 20%~90%，误差 <7% |
| 8 | §一·1 设计指标表 | 仅按题目列基本 60cm / 发挥 110cm 两档、误差 ≤10% | 补充最终验证距离 130cm、误差实测均 <7% 口径 |
| 9 | §二·4 距离估算 | 在 d = 1.1m（发挥部分）时，路径损耗增加约 5.2dB；均在 1.1m 以内 | 在 d = 1.3m（最终验证 130cm）时，路径损耗增加约 6.7dB；均在 1.3m 以内 |
| 10 | §四·1.2 发挥测试方案 | 距离扩展 110cm／收发间距扩至 110cm | 距离扩展 130cm／收发间距扩至 130cm |
| 11 | §四·3.2 发挥测试结果表 | 传输距离 110cm｜≥110cm｜112.0 cm｜达标 | 传输距离 130cm｜130cm 距离完整 AM 调制传输，解调输出无明显失真｜达标（110cm/112cm 保留为原始记录） |
| 12 | §四·3.3 测试结论 | 60cm 传输距离…误差小于 10%；频率误差远小于 10%；发挥部分 110cm 距离 | 误差均 <7%；130cm 距离完整 AM 调制传输 |
